require "spec_helper"

describe Nerdinator do
  it "has a version number" do
    expect(Nerdinator::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
